--VER ARCHIVOS POR TIPO Y UBICACIÓN
SELECT NAME BD, PHYSICAL_NAME UBICACION,RIGHT(physical_name,3) TIPO, SIZE TAMAÑO,STATE_DESC ESTADO,
CASE WHEN IS_READ_ONLY=0 THEN 'NO' ELSE 'SI' END [SOLO LECTURA]
FROM SYS.MASTER_FILES
